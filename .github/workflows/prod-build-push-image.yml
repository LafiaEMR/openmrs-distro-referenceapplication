name: Production Build, Push and Deploy

on:
  push:
    branches: prod-test

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set Short SHA
        run: |
          echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_ENV

      # - name: Build Docker Images from Docker Compose FIle
      #   run: |
      #     # docker compose build -t repo_name:${{ env.SHORT_SHA }} .
      #     # docker compose push
      #     TAG=${{ env.SHORT_SHA }} docker compose build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-2

      # - name: Login to ECR
      #   id: ecr-login
      #   uses: aws-actions/amazon-ecr-login@v1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Push Docker Images
        env:
          TAG: ${{ env.SHORT_SHA }}
        run: |
          docker compose build
          docker compose push

      # - name: Login to ECR
      #   run: |
      #     aws ecr get-login-password --region eu-west-2 | docker login --username AWS --password-stdin 571600871041.dkr.ecr.eu-west-2.amazonaws.com

      # - name: Push to ECR
      #   run: |
      #     # docker compose push repo_name:${{ env.SHORT_SHA }}
      #     TAG=${{ env.SHORT_SHA }} docker compose push
